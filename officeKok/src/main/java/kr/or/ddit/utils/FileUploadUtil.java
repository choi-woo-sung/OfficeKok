package kr.or.ddit.utils;

import java.io.IOException;
import java.util.List;

import kr.or.ddit.vo.PostAttVO;
import kr.or.ddit.vo.PostVO;

public class FileUploadUtil {
//	private <T> boolean processAttatchFiles(T postvo) throws IOException {
////		// 첨부 파일 존재 여부 확인
////		// 2. Attatch insert n번
//////		postvo.
//////		List<T> attatchList = ((T) postvo).getAttatchList();
//////		if (attatchList == null || attatchList.size() == 0)
//////			return true;
//////		int rowcnt = postattDAO.insertAttatchs(postvo); // meta
//////		if (rowcnt > 0) {
//////			for (PostAttVO attatch : attatchList) {
//////				attatch.saveFile(saveFolder);
//////			}
//////		}
////		return rowcnt > 0;
//	}
}
